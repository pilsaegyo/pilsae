# 필새 영상 아카이브 — 날짜 자동 재분석 버전

`videos.json` 원본을 매번 그대로 읽은 뒤 `description + source + title` 전체를 다시 분석하여 날짜를 추출합니다.

## 인식 형식
- `150227` → 2015-02-27
- `010826` → 2001-08-26
- `20010812` → 2001-08-12
- `2015.02.27`
- `2015-02-27`
- `2015/02/27`
- `2015년 2월 27일`
- `2000.06` → 화면에는 2000.06, 내부 정렬값은 2000-06-01
- `2000년 6월` → 화면에는 2000.06
- `00\`` → 화면에는 2000년, 내부 정렬값은 2000-01-01
- `99\`` → 화면에는 1999년
- `1999\`` → 화면에는 1999년
- `99년도`, `1999년` → 연도만 적용

## 2자리 연도 변환 규칙
- 00~29 → 2000~2029
- 30~99 → 1930~1999

## 연도/월만 있는 값 처리
실제 월/일을 알 수 없으므로 화면에는 없는 날짜를 만들지 않습니다.
정렬 및 연도 필터를 위해 내부값만 다음처럼 임시 처리합니다.
- 연도만: YYYY-01-01
- 연/월만: YYYY-MM-01

## 혼합 영상
한 설명에서 2개 이상의 날짜가 추출되면 자동으로 `혼합 영상`으로 분류합니다.

## 정렬
1. 원본/설명에서 얻은 날짜 기준 내림차순
2. 같은 날짜면 YouTube 업로드 날짜 기준 내림차순


## AI 영상 리스트 제외
사이트 표시 단계에서 제목에 아래 문구가 포함된 영상은 자동 제외합니다.
- `Ai 데뷔초 신혜성`
- `Ai 신혜성`

대소문자는 구분하지 않습니다. 따라서 `AI 신혜성`, `ai 신혜성`도 제외됩니다.

원본 `data/videos.json`은 수정하지 않습니다.


## 날짜 표시 정리 규칙
- `99\``, `00\``, `1999\`` 같은 연도만 추정되는 형식은 자동 날짜로 만들지 않음 → `날짜 미확인`
- `2000.06`처럼 일자가 없는 값도 자동 날짜로 만들지 않음 → `날짜 미확인`
- 정확한 날짜 형식만 인식:
  - `150227`
  - `20010812`
  - `2015.02.27`
  - `2015-02-27`
  - `2015/02/27`
  - `2015년 2월 27일`
- `1998-10-25 · 19981025`처럼 같은 날짜가 중복 추출돼도 화면에는 `1998-10-25` 한 번만 표시
- 여러 날짜가 있어도 같은 연도라면 화면에는 그 연도에서 최신 날짜 1개만 표시
- 서로 다른 연도가 여러 개면 연도별 1개씩 표시하며 `혼합 영상`으로 분류


## 날짜 추출 범위 변경
날짜는 이제 **동영상 description 필드에서만** 추출합니다.

다음 값은 날짜 판별에 사용하지 않습니다.
- `title`
- `source`
- YouTube 업로드일 `publishedAt`

따라서 제목에 `1999`, `2001`, `2015` 등이 들어 있어도 설명에 해당 날짜가 없으면 날짜로 인식하지 않습니다.


## 부분 날짜 지원
날짜는 여전히 `description`에서만 추출합니다.

추가 인식:
- `2000.06` → 화면에 `2000.06`
- `2000년 6월` → 화면에 `2000.06`
- `1999년` → 화면에 `1999년`
- `2002년` → 화면에 `2002년`

정렬을 위해 내부적으로는:
- 연도만: `YYYY-01-01`
- 연/월: `YYYY-MM-01`

화면에는 임의의 월/일을 표시하지 않습니다.

같은 연도에서 여러 값이 잡히면 1개만 표시하며,
정확도가 높은 값을 우선합니다:
정확한 날짜 > 연월 > 연도


## 카드형 / 목록형 보기 전환
검색 결과 우측에 보기 전환 버튼을 추가했습니다.

- `▦` : 기존 카드형 보기
- `☰` : 목록형 보기

목록형에서는:
- 작은 썸네일
- 영상 제목
- 날짜

만 간결하게 표시합니다.

선택한 보기 방식은 브라우저 localStorage에 저장되어 다음 방문에도 유지됩니다.


## 해시태그 날짜 제외
날짜 분석은 `description`에서만 수행하며,
`#`로 시작하는 해시태그 토큰은 분석 전에 제거합니다.

예:
- `#1999` → 날짜 인식 안 함
- `#2000년` → 날짜 인식 안 함
- `#2015-02-27` → 날짜 인식 안 함
- 일반 본문 `2015-02-27` → 날짜 인식함


## UX 개선 사항
- 원본 날짜 / 업로드 날짜 / 제목 기준 정렬 추가
- 연도 선택에 해당 연도 영상 개수 표시
- 필터 초기화 버튼 추가
- 초기 로딩 상태 추가
- 한 번에 60개씩 렌더링하고 `더 보기` 방식 적용
- 영상 제목도 클릭하면 YouTube로 이동
- 검색 결과에서 날짜 미확인 개수 표시
- 공개 방문자에게 관리자 버튼 숨김
  - `?admin=1` 또는 `#admin`으로 접근한 경우에만 관리자 진입 버튼 표시
- 검색 placeholder를 실제 검색 범위에 맞게 수정


## 채널 브랜딩 헤더 반영
- 사이트 상단 hero 영역이 YouTube 채널 배너 이미지를 배경으로 사용합니다.
- 채널 프로필 이미지는 원형 아바타로 자동 반영됩니다.
- `YouTube 영상 정보 업데이트`를 누르면 `data/videos.json`뿐 아니라 `site-config.json`의 `bannerImageUrl`, `profileImageUrl`, `channelTitle`도 함께 갱신됩니다.
- 기본 파비콘은 `assets/favicon-p.png`(연보라 배경 + 흰색 P)입니다.


## v5 디자인 개선
- 전체 톤을 화이트 + 연보라 아카이브 스타일로 정돈
- 채널 헤더에 전체 영상 / 날짜 인식 / 확인 필요 요약 배지 추가
- 검색·연도·유형·정렬을 하나의 필터 패널로 통합
- 모바일에서는 배너를 숨기고 프로필/제목을 더 작게 표시
- 모바일 필터는 검색창만 기본 노출하고 나머지는 `필터` 버튼으로 펼침
- 활성화된 필터 개수를 필터 버튼에 표시
- 영상 카드, 목록형, 버튼, 관리자 카드까지 라벤더 계열로 통일


## v6 수정
- Android 모바일에서 영상 썸네일/제목/YouTube 버튼 클릭 시 YouTube 앱을 우선 실행함.
- iPhone/iPad에서도 YouTube 앱 URL을 우선 시도하고, 앱이 없으면 웹 YouTube로 이동함.
- `170113`, `170112`, `170114`, `170115`처럼 같은 연도에 여러 정확한 날짜가 있어도 모두 표시함.
- `혼합 영상` 판정도 이제 '연도가 여러 개인 영상'이 아니라 '서로 다른 날짜가 2개 이상인 영상' 기준임.
- 동일 날짜의 중복 표기만 제거함.


## v7 통합 개선
- 혼합영상은 서로 다른 연도가 2개 이상일 때만 분류함. 같은 연도 내 여러 날짜는 해당 연도 영상으로 유지함.
- 같은 연도 날짜는 연도를 반복하지 않고 압축 표시하며, 많은 날짜는 `+N개`로 접어둠.
- 혼합영상은 먼저 연도 요약을 보여주고 세부 날짜를 펼쳐볼 수 있음.
- 혼합영상도 포함된 각 연도 필터 결과에 정상 노출됨.
- 제목/설명/출처/날짜 모두 검색 대상임.
- 활성 필터 칩과 개별 해제 기능 추가.
- 결과 영역에서 `전체 N개 · 현재 결과 N개`를 명확히 구분함.
- 모바일은 헤더 통계를 전체 영상 하나만 남기고 더 압축함.
- 관리자 화면에 날짜 미확인 영상 전용 검토 목록, 미확인 사유, 설명 미리보기, YouTube 원본 링크 추가.
- 실제 YouTube 설명을 OAuth로 수정하는 기능은 다음 단계로 보류함.


## v7.1 날짜 접기 버그 수정
- `hidden` 속성이 `.date-extra { display: contents }`에 의해 무시되던 문제 수정.
- 데스크톱은 날짜 3개까지만 기본 표시, 나머지는 `+N개`.
- 모바일은 날짜 2개까지만 기본 표시, 나머지는 `+N개`.
- `+N개` 클릭 시 전체 펼침, `접기` 클릭 시 다시 축소.
- 카드형/목록형 모두 동일하게 적용.


## v8 구조 개선
- 카드형 / 목록형 / 연도·월 아카이브형의 3가지 보기 제공.
- 아카이브형은 각 영상을 대표 원본 날짜 기준으로 연도 → 월 순으로 묶어 보여줌.
- 혼합영상은 서로 다른 연도가 2개 이상일 때만 분류.
- 혼합영상 카드의 긴 연도 목록은 최대 6개까지만 요약하고, 날짜는 데스크톱 3개 / 모바일 2개까지만 기본 노출.
- 날짜 더보기 버튼은 연도 요약과 별개로 실제 숨겨진 날짜 개수만 표시.
- 관리자 화면은 `영상 동기화 / 날짜 검토 / 사이트 설정` 탭으로 분리.
- YouTube 설명 직접 수정(OAuth)은 이번 버전에서 제외.


## v8.1 날짜 정확도별 아카이브 그룹화
- `day`: 정확한 날짜의 해당 월에 배치.
- `month`: 해당 월에 배치하고 `일자 미상` 표시.
- `year`: 1월로 넣지 않고 해당 연도 하단의 `연도만 확인` 영역에 배치.
- `unknown`: 전체 아카이브 마지막의 `날짜 미확인` 영역에 배치.
- 연도만 있는 영상은 내부적으로 YYYY-01-01을 저장해도 화면 그룹화는 `precision` 기준으로 처리하므로 1월 영상으로 오해되지 않음.


## v9 1차 사용성 개선
1. 검색 결과에 일치 문맥과 하이라이트 표시. 제목/출처/설명/날짜 중 왜 검색됐는지 확인 가능.
2. 연도·월 아카이브 보기에서 연도 빠른 이동 바 추가.
3. 검색/연도/유형/정렬/보기 상태를 URL 쿼리스트링에 저장해 북마크·공유·뒤로가기를 지원.
4. 보기 모드 저장 이력이 없는 모바일에서는 목록형을 기본값으로 사용. 사용자가 바꾸면 기존처럼 선택을 저장.
5. 초기 데이터 로딩 중 실제 카드 형태의 skeleton UI 표시.


## v9.1 보기 전환 버그 수정
- 카드형 / 목록형 / 연도·월 보기 버튼을 눌렀을 때 URL의 기존 `view` 값이 새 선택을 덮어쓰던 문제 수정.
- 보기 전환 시 선택한 모드를 URL에 먼저 반영한 뒤 렌더링하도록 변경.


## v10 날짜 인식 개선
- `08.11.08`, `08-11-08`, `08/11/08` 같은 `YY.MM.DD` 형식을 정확한 날짜로 자동 인식.
- `9905` 같은 4자리 `YYMM`은 자동 적용하지 않음. 설명의 한 줄 맨 앞에 있고 MM이 01~12일 때만 관리자 `날짜 후보`로 제시.
- 관리자가 `1999.05로 적용`을 누르면 Worker가 GitHub `data/videos.json`에 수동 월 날짜를 저장하고 재배포.
- `날짜 아님`을 누르면 해당 후보를 제외 목록에 저장해 다음 동기화 이후에도 다시 제안하지 않음.
- 수동 적용 날짜와 제외 후보는 YouTube 전체 동기화 시에도 보존.


## v10.1 제목 + 설명 날짜 후보
- 날짜 미확인 영상에 한해 제목도 관리자 검토 후보 탐색에 사용.
- 제목 `08.11.08 ...` → `2008.11.08` 후보(자동 확정 아님).
- 제목 `081108 ...` → `2008.11.08` 후보.
- 제목 `9905 ...` → `1999.05` 후보.
- 설명의 `YY.MM.DD`는 기존처럼 자동 확정.
- 설명의 `YYMM` 4자리는 보수적으로 줄 맨 앞에서만 월 후보 생성.
- 관리자 화면에 `제목에서 발견` / `설명에서 발견` 배지 표시.
- 제목에서 나온 날짜는 반드시 관리자가 `적용`해야 실제 아카이브 날짜가 됨.


## v11 수동 날짜 + 플레이리스트
- 제목이 `2007 콘텐츠명`처럼 4자리 연도로 시작하면 `2007년` 후보로 처리. `2020.07`처럼 YYMM으로 오해하지 않음.
- 관리자 날짜 검토에서 날짜 후보가 없어도 직접 `2007`, `2007.05`, `2007.05.21` 형식으로 입력해 적용 가능.
- 콘텐츠 유형을 날짜 분류와 독립된 `일반 영상 / 플레이리스트`로 추가.
- 관리자에서 모든 영상을 검색해 플레이리스트로 지정/해제 가능.
- 연도가 있는 플레이리스트는 해당 연도/월 아카이브에도 표시되며 플레이리스트 필터로도 별도 탐색 가능.
- 날짜 없는 플레이리스트는 `날짜 미확인`이 아니라 아카이브의 `플레이리스트 > 연도 미지정`에 별도 표시.
- 전체 동기화 후에도 수동 날짜, 날짜 후보 제외 설정, 플레이리스트 지정이 유지됨.


## v12 다중 연도 + 플레이리스트 후보 축소
- 수동 날짜 입력에 `2007, 2006`처럼 여러 연도를 한 번에 입력 가능.
- 서로 다른 연도가 2개 이상 적용되면 자동으로 `혼합 연도` 분류.
- 기존의 `2007`, `2007.05`, `2007.05.21` 단일 입력도 그대로 지원.
- YouTube 동기화 시 영상 길이(`contentDetails.duration`)를 함께 저장.
- 플레이리스트 분류 관리 영역에는 `기존 플레이리스트` 또는 `6분 이상 영상`만 표시.
- 검색하지 않은 상태에서는 최대 30개까지만 보여주고 길이순으로 정리.


## v13 관리자 화면 개선
- `콘텐츠 분류`를 `날짜 검토`에서 분리해 독립 관리자 탭으로 이동.
- `?admin=1`로 들어오면 관리자 패널을 페이지 최상단으로 자동 이동하고 즉시 열도록 변경.
- 더 이상 공개 영상 목록 아래까지 스크롤할 필요 없음.
- 관리자 상단에 `일반 아카이브 보기` 바로가기 추가.
- 콘텐츠 분류 영역의 검색/설명/목록 배치를 정리해 버튼과 설명이 겹치지 않도록 개선.


## v14 다년도 플레이리스트
- 플레이리스트에 `다년도` 범위를 별도로 저장 가능.
- 날짜가 없어도 관리자가 `다년도로 지정`하면 `연도 미지정`/날짜 검토 대상에서 제외.
- 날짜가 실제로 2개 이상의 서로 다른 연도를 포함하는 플레이리스트도 자동으로 다년도 취급.
- 타임라인에 `플레이리스트 > 다년도 플레이리스트` 영역을 별도 표시.
- 진짜 연도를 정할 수 없는 전체 모음만 `플레이리스트 > 연도 미지정`에 남김.
- 관리자 날짜 검토 카운트에는 일반 영상의 날짜 미확인만 집계.
- 콘텐츠 분류 탭의 플레이리스트 항목에서 `다년도로 지정 / 연도 미지정` 선택 가능.
- 동기화 후에도 playlistScope 값 유지.


## v14.1 기존 날짜가 있는 다년도 플레이리스트
- 이미 `2001` 등 단일 날짜가 저장된 플레이리스트도 `다년도로 지정` 가능.
- `playlistScope = multi-year`가 기존 대표 날짜보다 우선하도록 변경.
- 따라서 기존 날짜를 삭제하지 않아도 타임라인에서는 `플레이리스트 > 다년도 플레이리스트`로 이동.
- 관리자 콘텐츠 분류 목록에서 기존 대표 날짜가 남아 있는 경우 참고용으로 표시.


## v14.2 콘텐츠 분류 버튼/UI 정리
- 관리자 `콘텐츠 분류` 탭에 아이콘 추가.
- `플레이리스트로 지정`, `다년도로 지정`, `연도 미지정`에 각각 독립 아이콘과 텍스트 표시.
- 기존 버튼들이 한 줄/좁은 열에서 겹치던 레이아웃을 카드별 하단 액션 영역으로 분리.
- 태블릿/모바일에서는 액션 영역을 영상 정보 아래 전체 너비로 이동.
- 작은 모바일에서는 다년도/연도 미지정 버튼을 자동으로 한 줄씩 배치.


## v14.3 수동 날짜 이후 설명 변경 감지
- 수동 날짜가 지정된 영상의 YouTube 설명이 수정되면 다음 API 동기화에서 변경을 감지.
- 기존 수동 날짜는 `previousManualDates`로 보관하고 활성 날짜에서 잠시 제외.
- 새 설명에 날짜가 있으면 그 날짜를 우선 표시.
- 새 설명에 날짜가 없으면 다시 `날짜 검토` 대상으로 이동.
- 관리자에서 이전 수동 날짜와 새 설명에서 인식된 날짜를 함께 확인 가능.
- `새 설명 날짜 사용` 버튼으로 새 설명 기준을 확정 가능.
- 새 수동 날짜를 다시 입력하면 검토 상태가 자동 해제됨.


## v14.4 콘텐츠 분류 목록 폭 개선
- 콘텐츠 분류 목록을 썸네일/영상정보/액션으로 다시 배치.
- 중간 화면에서는 액션 영역을 영상 정보 아래 전체 너비로 내려 겹침 방지.
- 넓은 화면에서는 우측 액션 영역을 420~520px까지 확보.
- `플레이리스트로 지정`, `다년도로 지정`, `연도 미지정` 버튼의 최소 폭을 확보하고 텍스트 줄바꿈 허용.
- 콘텐츠 분류 목록의 강제 높이/스크롤 제한을 제거해 좁은 박스 안에서 겹치지 않도록 개선.


## v14.5 다년도 플레이리스트 표시 수정
- `playlistScope = multi-year`인 플레이리스트가 카드/목록에서 `날짜 미확인` 또는 `날짜 확인 필요`로 표시되던 문제 수정.
- 날짜 값이 없어도 `다년도` + `여러 연도 수록`으로 표시.
- 날짜 유형 필터에서는 다년도 플레이리스트를 `혼합 연도` 쪽으로 취급하고 `날짜 미확인`에서는 제외.
- 연도 미지정 플레이리스트는 `연도 미지정`으로 별도 표시.


## v15 사용성 정리
- 공개 헤더 통계에서 `날짜 인식 / 확인 필요`를 제거하고 전체 영상 기록 수만 노출.
- 공개 검색 결과에서도 관리 성격의 `날짜 미확인 N개` 문구 제거.
- `?admin=1` 관리자 모드에서는 공개 검색/필터/영상 목록을 숨겨 전용 관리자 화면처럼 표시.
- 타임라인의 다년도/연도 미지정 플레이리스트를 하나의 `플레이리스트` 허브로 통합.
- 날짜 검토 탭에 `날짜 확인 필요 / 설명 변경 재검토`를 각각 카운트하고 0건일 때 완료 상태 표시.
- 카드/목록/연도·월 보기 전환 버튼에 텍스트 라벨을 추가하고 모바일 간격을 개선.


## v15.1 날짜 미확인 카운트/캐시 정리
- 공개 결과 상단에는 `전체 N개 · 현재 결과 N개`만 표시하고 `날짜 미확인 N개`는 완전히 제거.
- 다년도 플레이리스트의 실제 필터 분류(`혼합 연도`)와 raw 데이터 상태가 달라도 공개 카운트가 어긋나지 않도록 정리.
- `style.css?v=15.1`, `script.js?v=15.1`로 캐시 버스터를 추가해 모바일 브라우저가 예전 JS/CSS를 계속 쓰는 문제를 방지.
- `<body data-build="15.1">` 빌드 마커 추가.


## v16 찾기/탐색 사용성 개선
- 모바일에서 검색/필터 패널을 sticky로 고정해 목록을 내려본 뒤에도 바로 검색 가능.
- 검색어 2자 이상 입력 시 제목/출처/날짜 기반 자동제안 최대 6개 표시.
- 연도·월 보기의 연도 버튼에 해당 연도의 영상 수 표시.
- `플레이리스트` 빠른 진입 버튼과 플레이리스트 총 개수 표시.
- 카드 정보 순서를 `날짜 → 제목 → 출처` 중심으로 재정리.
- 플레이리스트 배지를 `플레이리스트 · 다년도`처럼 하나로 합쳐 배지 중복 감소.
- CSS/JS 캐시 버전 `v=16` 적용.


## v17 탐색 정리
- 메인 `플레이리스트 빠른 진입` 버튼 제거. 플레이리스트는 기존 `콘텐츠 유형` 필터에서만 선택.
- 검색창에 `×` 지우기 버튼 추가.
- 검색 자동제안에서 ↑/↓ 이동, Enter 선택, Esc 닫기 지원.
- 결과가 0개일 때 현재 검색/필터 조건을 보여주고 `검색·필터 초기화` 버튼 제공.
- 긴 목록 탐색 후 바로 위로 돌아갈 수 있도록 우측 하단 `↑` 버튼 추가.
- CSS/JS 캐시 버전 `v=17` 적용.


## v18 실험형 탐색 기능
- 별도 버튼을 늘리지 않고 카드의 `날짜` 자체를 클릭하면 같은 날짜 영상 검색.
- 카드의 `출처` 텍스트 자체를 클릭하면 같은 출처 영상 검색.
- 검색 중일 때만 `제목 일치 / 날짜 일치 / 출처 일치 / 설명 일치`를 작은 라벨로 표시.
- `연도·월` 보기에서만 각 연도 아래 월별 건수 칩 표시. 클릭하면 해당 월로 이동.
- 기존 기본 화면의 요소 수는 거의 늘리지 않는 방향으로 적용.
- CSS/JS 캐시 버전 `v=18`.
- 테스트 후 별로면 v17의 `index.html / script.js / style.css`로 바로 롤백 가능.


## v18.2 모바일 검색 자동제안 축약
- 실제 검색 결과 카드가 아니라 **검색 입력 중 표시되는 자동제안 목록**에 적용.
- 모바일에서 자동제안이 4개 이상이면 처음 3개만 표시.
- `N개 더보기`를 누르면 자동제안 드롭다운만 최대 52vh 높이로 확장되고 내부 스크롤로 탐색.
- `접기`를 누르면 다시 3개만 표시.
- PC에서는 기존처럼 자동제안 전체 표시.
- 실제 검색 결과 목록은 v18 방식 그대로 유지.
- CSS/JS 캐시 버전 `v=18.2`.


## v18.3 자동제안 더보기 사라짐 수정
- 모바일 자동제안의 `더보기/접기` 클릭을 검색 UI 내부 클릭으로 처리.
- 기존에는 자동제안 드롭다운이 `.search-field` 바깥에 있어, 더보기 클릭 직후 바깥 클릭으로 오인되어 닫히는 문제가 있었음.
- 실제 검색 결과 목록 동작은 변경 없음.
- 캐시 버전 `v=18.3`.


## v18.4 사이트 타이틀 보존 + 설명 검색 3줄
- YouTube 영상 동기화 시 `site-config.json`의 사용자 지정 사이트 타이틀을 절대 YouTube 값으로 덮어쓰지 않도록 Worker 보강.
- 이후 배포용 ZIP에는 `site-config.json`과 `data/videos.json`을 포함하지 않아 사용자 설정/관리 데이터를 실수로 덮어쓰는 일을 방지.
- 모바일 검색에서 `설명 일치`인 경우 설명 미리보기를 온전한 3줄로 표시.
- 설명 검색 스니펫 범위를 늘려 3줄을 채울 만큼 충분한 문맥을 제공.
- 출처/날짜 등 다른 검색 스니펫은 기존 밀도를 유지.
- 캐시 버전 `v=18.4`.


## v19 관리자 페이지 분리
- 공개 `index.html`에서 관리자 UI 전체 제거.
- 관리자 전용 경로 `/admin/` 추가 (`admin/index.html`).
- 기존 `/?admin=1` 또는 `#admin` 접근은 `/admin/`으로 자동 이동.
- 공개 페이지 푸터의 숨김 관리자 진입 버튼 제거.
- 관리자 페이지에서는 공개 검색/영상 목록을 보여주지 않고 관리자 기능만 표시.
- 기존 Cloudflare `ADMIN_TOKEN` 인증과 관리자 API는 그대로 사용.
- `site-config.json`, `data/videos.json`은 업데이트 패치에 포함하지 않음.
- Worker 변경 없음.


## v19.1 공개 첫 진입 오류 수정
- 관리자 페이지 분리 후 공개 `index.html`에는 존재하지 않는 `#faviconInput`, `#syncYoutubeVideos`에 이벤트를 연결하려다 `bindEvents()`가 중단되던 문제 수정.
- 데이터 자체는 이미 로드된 뒤 예외가 발생해, 첫 화면에서는 오류가 보이지만 이후 보기 전환 시 목록이 다시 나타나는 현상이었음.
- 관리자 전용 요소의 이벤트 바인딩을 optional 처리하여 공개 페이지 초기화가 끝까지 정상 진행되도록 수정.
- Worker 변경 없음.


## v19.2 타이틀 깜빡임 + 모바일 자동제안 더보기 수정
- 공개 페이지 첫 페인트에서 HTML에 적힌 기본 타이틀을 잠시 숨기고, `site-config.json`의 저장된 타이틀을 적용한 뒤 표시하도록 변경.
- 따라서 새로고침 시 기본 문구가 잠깐 보였다가 사용자 지정 타이틀로 바뀌는 현상 제거.
- 자동제안 `더보기` 클릭 시 같은 click 이벤트 안에서 드롭다운 DOM이 다시 그려지면서, 바깥 클릭으로 잘못 판정되어 즉시 닫히던 문제 수정.
- 바깥 클릭 판정을 `event.composedPath()` 기준으로 보강하여 재렌더링 이후에도 해당 클릭이 자동제안 내부 클릭임을 정확히 유지.
- Worker 변경 없음.


## v19.3 KoPubWorld 돋움 타이포그래피 개선
- 무료 웹폰트 `KoPubWorld Dotum`을 jsDelivr CDN으로 적용.
- 본문/검색/필터/카드/관리자 UI 전체에 동일한 한글 폰트 스택 적용.
- 한글 제목의 과한 자간을 조금 완화하고 모바일 카드 제목/설명 행간을 보정.
- 폰트 로딩 실패 시 Pretendard → 시스템 폰트 순으로 자동 대체.
- 외부 CDN 연결 지연을 줄이기 위해 jsDelivr preconnect 추가.
- 레이아웃/기능/색상은 변경하지 않음.
- Worker 변경 없음.


## v19.4 Pretendard + 헤더 정리
- KoPubWorld 적용을 제거하고 Pretendard 중심 폰트 스택으로 복귀.
- 전체 자간/행간/굵기를 조금 더 정돈해 기존보다 가볍고 세련된 인상으로 보정.
- PC 헤더에서 아카이브명과 `@pilsae`가 따로 노는 느낌을 줄이도록 하나의 브랜드 락업처럼 재배치.
- PC에서는 아카이브명 오른쪽에 얇은 구분선을 두고 핸들을 작고 차분하게 배치.
- 모바일에서는 공간이 좁아지는 점을 고려해 핸들을 작은 pill 형태로 유지.
- 기능/데이터/Worker 변경 없음.


## v19.5 사이트 안내
- 최상단 헤더 우측에 `?` 도움말 아이콘 추가.
- PC에서는 우측 상단의 작은 팝오버 형태로 사이트 기능 안내 표시.
- 모바일에서는 가독성을 위해 하단 시트 형태로 표시하고 배경 dim 처리.
- 검색, 연도·월, 날짜 클릭, 출처 클릭, 플레이리스트의 핵심 사용법만 간략히 안내.
- ESC, 닫기 버튼, 바깥 영역 클릭으로 닫기 지원.
- 관리자 페이지에는 도움말 아이콘을 추가하지 않음.
- Worker 변경 없음.


## v20 사용자 탐색 개선
- 검색 결과를 `제목 정확 일치 → 제목 시작/부분 일치 → 날짜 → 출처 → 설명` 우선순위로 정렬.
- 검색 중에도 사용자가 선택한 정렬값은 동일 관련도 그룹 안의 보조 정렬로 유지.
- 사이트 안내(`?`) 하단에 `현재 화면 링크 복사` 추가. 검색어/필터/정렬/보기 상태가 포함된 현재 URL을 그대로 공유 가능.
- 결과가 0개일 때 현재 걸린 조건에 맞춰 `검색어 지우기`, `연도 해제`, `날짜 유형 해제`, `콘텐츠 유형 해제`, `전체 보기`를 선택적으로 표시.
- 화면 구성은 거의 그대로 유지하고 탐색 복구성과 검색 품질 위주로 개선.
- Worker 변경 없음.


## v20.1 공유 접근성 + 다년도 플레이리스트 표시 수정
- `현재 화면 링크 복사`를 도움말 안에만 두지 않고 결과 영역 우측에 바로 접근 가능한 작은 버튼으로 추가.
- PC에서는 `↗ 링크 복사`, 모바일에서는 공간을 아끼기 위해 아이콘 버튼으로 표시.
- 다년도 플레이리스트의 연도·월 보기에서 `날짜 미확인 외 N개 날짜`처럼 어색하게 보이던 문구 제거.
- 다년도 플레이리스트는 `여러 연도 수록` + `다년도`로 표시하고, 확인 가능한 연도 목록이 있으면 아래에 연도만 요약.
- 연도 미지정 플레이리스트도 `연도 미지정`으로 명확하게 표시.
- Worker 변경 없음.


## v20.2 UI 정리
- `현재 화면 링크 복사` 기능을 일반 화면과 사이트 안내에서 모두 제거.
- 모바일/PC 결과 영역의 공유 아이콘·버튼을 제거해 보기 전환 영역을 다시 단순화.
- v20의 검색 결과 우선순위 개선, 결과 없음 복구 기능은 그대로 유지.
- v20.1의 다년도 플레이리스트 연도·월 표시 수정도 그대로 유지.
- 결과 헤더 간격을 조금 정리해 모바일에서도 보기 버튼이 자연스럽게 우측 정렬되도록 보정.
- Worker 변경 없음.


## v20.3 PC 히어로 핸들 위치 조정
- PC에서 메인 타이틀 위의 YouTube 채널 핸들(`@pilsae`)을 기존보다 살짝 아래로 이동.
- 폰트 크기를 더 작게 조정해 메인 타이틀보다 시선이 덜 가도록 정리.
- 모바일 레이아웃은 변경하지 않음.
- Worker 변경 없음.


## v21 관리자 1차 운영 개선
- 관리자 첫 탭을 `대시보드`로 변경.
- 대시보드에 전체 영상, 날짜 검토 필요, 설명 변경 재검토, 플레이리스트, 날짜 미확인, 최근 YouTube 동기화 시간 표시.
- `데이터 건강검사` 추가: 중복 ID, 썸네일 없음, 출처 없음, 날짜 미확인, 설명 변경 재검토, 날짜 형식 이상, 플레이리스트 분류 확인 자동 점검.
- YouTube 동기화를 `변경사항 미리보기 → 변경사항 적용` 2단계로 변경.
- 미리보기에서 신규/제목 변경/설명 변경/삭제 후보/변경 없음 수와 최대 30건의 상세 변경 표시.
- Worker `/sync-videos`가 `previewOnly:true`를 받으면 GitHub를 수정하지 않고 비교 결과만 반환.
- 실제 적용 시 기존 수동 날짜/콘텐츠 분류 보존 로직은 그대로 유지.


## v22 날짜 검토 큐 + 변경 이력
- 날짜 검토 화면을 원인별 큐로 개선: `전체 / 설명 변경 / 날짜 후보 / 표기 확인 / 날짜 정보 없음`.
- 기본 정렬은 `설명 변경 → 날짜 후보 → 날짜 표기 확인 → 날짜 정보 없음`, 같은 그룹은 최신 업로드 우선.
- 최신/오래된 업로드순 정렬도 선택 가능.
- 각 검토 카드에 검토 이유 요약을 추가해 왜 목록에 들어왔는지 바로 확인 가능.
- 관리자 탭에 `변경 이력` 추가. 최근 100건을 목록으로 조회.
- 기록 대상: 실제 YouTube 동기화 적용, 수동 날짜, 후보 날짜 적용/제외, 새 설명 날짜 사용, 콘텐츠 유형, 플레이리스트 범위, 사이트 설정.
- 변경 이력은 `data/admin-history.json`에 별도 저장하며 `videos.json` 구조에는 영향을 주지 않음.
- 이력 저장 실패는 이미 성공한 원본 변경을 실패 처리하지 않도록 best-effort 방식으로 동작.
- Worker 변경 있음.


## v22.1 변경 이력 되돌리기
- v22 기능을 모두 포함한 누적 버전.
- 변경 이력의 안전한 항목에 `되돌리기` 버튼 추가.
- 지원 범위: 수동 날짜 적용, 날짜 후보 적용, 후보 제외, 새 설명 날짜 사용, 콘텐츠 유형 변경, 플레이리스트 범위 변경.
- YouTube 전체 동기화와 사이트 설정 변경은 자동 되돌리기 대상에서 제외.
- 되돌리기 전에 현재 데이터가 해당 이력의 `after` 상태와 같은지 확인하여, 이후 추가 수정이 있는 오래된 이력이 최신 값을 덮어쓰지 못하도록 보호.
- 되돌리기 자체도 `변경 되돌리기` 이력으로 다시 기록.
- Worker 변경 있음.


## v22.2 플레이리스트 범위 우선순위 수정
- 관리자가 `다년도` 또는 `연도 미지정`을 직접 지정한 경우 해당 값이 자동 날짜 판정보다 우선하도록 수정.
- 기존에는 여러 연도의 날짜가 남아 있는 플레이리스트를 `연도 미지정`으로 바꿔도 자동 다년도 판정이 다시 덮어써 화면상 변경되지 않는 것처럼 보일 수 있었음.
- 이제 `다년도 → 연도 미지정 → 변경 이력에서 되돌리기` 테스트가 정상 동작하도록 명시적 `playlistScope`를 최우선으로 처리.
- Worker 변경 없음.


## v22.3 변경 이력 JSON 오류 수정
- v22/v22.1에서 `data/admin-history.json` 저장 시 실제 줄바꿈이 아니라 문자 `\n`이 JSON 뒤에 붙을 수 있던 문제 수정.
- 이 때문에 변경 이력 조회 시 `Unexpected non-whitespace character after JSON ...` 오류가 발생할 수 있었음.
- 앞으로는 정상 줄바꿈으로 저장.
- 이미 잘못 저장된 `admin-history.json`도 Worker가 끝의 잘못된 `\n`을 자동 보정해 읽을 수 있도록 복구 로직 추가.
- 다음 이력 저장 시 정상 JSON 형태로 다시 저장됨.
- Worker 변경 필수.


## v22.4 관리자 화면 인증 게이트
- `/admin/` 진입 시 ADMIN_TOKEN 인증 전에는 대시보드/날짜 검토/콘텐츠 분류/변경 이력/사이트 설정 UI를 숨김.
- 인증 화면만 노출되며, 올바른 토큰을 Worker `/admin-auth`에서 검증한 뒤 관리자 화면을 표시.
- 토큰을 입력했다고 바로 저장하지 않고, 서버 검증 성공 후에만 `sessionStorage`에 저장.
- 같은 브라우저 탭에서는 인증 상태를 유지하고, `로그아웃` 버튼으로 즉시 세션 토큰 제거 가능.
- 잘못된 저장 토큰은 자동 인증 실패 시 세션에서 제거.
- 실제 API 변경 권한은 기존과 동일하게 Worker의 ADMIN_TOKEN 검증으로 보호.
- Worker 변경 있음.


## v23 관리자 바로 처리 + 안전 백업/복원
- 대시보드의 날짜 검토, 설명 변경 재검토, 플레이리스트, 날짜 미확인, 최근 동기화 카드를 클릭형으로 변경.
- 클릭 시 해당 관리자 탭과 필터를 즉시 적용.
- 데이터 건강검사 유형 칩으로 목록 필터링 가능.
- 날짜 미확인/설명 변경/플레이리스트 분류 확인 항목은 `바로 확인`으로 해당 영상 처리 위치까지 이동.
- 콘텐츠 분류에 `전체 후보 / 플레이리스트만 / 미분류 후보만` 표시 필터 추가.
- 서버 복원 지점 최근 3개 유지.
- `지금 백업 만들기`로 수동 복원 지점 생성.
- YouTube 동기화 실제 적용 직전에 자동 백업 생성. 백업 생성 실패 시 동기화를 중단하여 기존 데이터를 보호.
- 백업 복원 전 현재 상태를 다시 자동 백업하여 복원 자체도 되돌릴 수 있는 안전 지점 확보.
- 복원 이력은 관리자 변경 이력에 기록.
- 새 파일은 Worker가 필요 시 자동 생성: `data/admin-backups-index.json`, `data/backups/videos-1.json` ~ `videos-3.json`.
- `data/videos.json`, `site-config.json`은 배포 ZIP에 포함하지 않음.
- Worker 변경 필수.


## v23.1 대시보드/건강검사 카운트 정합성 수정
- 대시보드 카운트와 날짜 검토 탭이 서로 다른 조건으로 계산되던 문제 수정.
- `날짜 검토 필요`, `설명 변경 재검토`, `날짜 미확인` 카운트를 실제 날짜 검토 큐와 동일한 기준으로 계산.
- `descriptionChangedAfterManual`도 날짜 검토 큐에 동일하게 포함.
- 플레이리스트의 설명 변경 이슈는 일반 날짜 검토 카운트에 섞지 않고 `플레이리스트 설명 변경 확인`으로 분리하여 콘텐츠 분류로 연결.
- 대시보드 숫자를 눌렀는데 대상 탭에서 0건으로 보이는 불일치 방지.
- Worker 변경 없음.


## v23.2 건강검사 새로고침 + 아이콘 정리
- `다년도` 또는 `연도 미지정`으로 관리자가 범위를 확정한 플레이리스트는 과거의 수동 날짜/설명 변경 상태가 남아 있어도 건강검사 경고에서 제외.
- 데이터 건강검사 제목 우측에 새로고침 아이콘 버튼 추가.
- 새로고침 시 현재 관리자 메모리 상태를 기준으로 건강검사를 즉시 다시 계산하여 이미 처리된 항목을 제거.
- 변경 이력의 `새로고침` 텍스트 버튼을 동일한 아이콘형 버튼으로 변경.
- 접근성을 위해 두 아이콘 버튼에 `aria-label`과 `title` 유지.
- Worker 변경 없음.


## v23.3 전체 초기화 시 최상단 이동
- 검색어 입력, 필터 변경, 정렬 변경, 개별 필터 해제 시에는 현재 스크롤 위치 유지.
- `초기화` 또는 빈 결과 화면의 `전체 보기`처럼 전체 조건을 초기화하는 경우에만 페이지 최상단으로 부드럽게 이동.
- Worker 변경 없음.


## v24 일반동영상 / Shorts 구분
- 공개 카드형/목록형 썸네일에 동영상 타입을 아이콘으로만 표시.
- 일반동영상은 가로 영상 플레이 아이콘, Shorts는 세로형 Shorts 아이콘 사용.
- 검색 필터에 `동영상 타입` 추가: 전체 동영상 / 일반 동영상 / Shorts.
- URL 상태에도 `format=standard|shorts`를 저장하여 필터 상태 유지.
- 관리자 `콘텐츠 분류`의 표시 옵션에 `동영상 타입 관리` 추가. 이 모드에서는 짧은 영상을 포함한 전체 영상 중 최대 50개를 관리 가능.
- 각 영상에서 `일반동영상 / Shorts`를 수동 지정 가능하며 수동 지정값은 이후 YouTube 동기화에서도 보존.
- 수동 타입 변경은 변경 이력에 기록되고 기존 되돌리기 기능도 지원.
- 자동 판별: YouTube Data API에는 Shorts 여부 전용 필드가 없어 `#shorts` 표기, 재생시간, 업로드일을 이용한 휴리스틱을 사용. 잘못 판별된 항목은 관리자에서 수정.
- 2024-10-15 이후 업로드된 3분 이하 영상은 Shorts 후보로 자동 판별하며, 60초 이하 영상도 Shorts 후보로 처리.
- Worker 변경 필수.


## v24.1 관리자 콘텐츠 목록 더보기
- `검색 전에는 최대 30개 / 검색하면 최대 50개` 안내 문구 제거.
- 콘텐츠 분류 목록은 검색 여부와 관계없이 처음 30개 표시.
- 목록 하단 `더보기` 버튼으로 30개씩 추가 로드.
- 검색어 또는 표시 모드를 변경하면 다시 첫 30개부터 표시.
- 남은 개수가 30개보다 적으면 버튼에 실제 남은 개수를 표시.
- Worker 변경 없음.


## v24.2 PC 상단 고정 / 초기화 스크롤 / 연도월 아이콘 점검
- PC에서는 검색·필터 영역이 스크롤 시 상단 헤더 아래에 고정되도록 복구.
- 일반 검색/필터/정렬/개별 필터 해제는 기존처럼 현재 스크롤 위치 유지.
- 전체 초기화(`필터 초기화`, 빈 결과의 `전체 보기`)만 최상단으로 부드럽게 이동하는 v23.3 로직 유지 확인.
- 카드형/목록형뿐 아니라 연도·월형 썸네일에도 일반동영상/Shorts 아이콘 표시.
- Worker 변경 없음.


## v24.3 PC 고정 검색영역 컴팩트 모드
- PC에서 검색/필터 영역이 실제 sticky 위치에 도달했을 때만 `컴팩트 모드`로 전환.
- 최상단에서는 기존 여유 있는 검색영역 유지.
- 고정 상태에서는 전체 영상 통계 카드를 숨기고, 필드 라벨/패딩/높이/간격을 축소.
- 901px 이상 PC에서는 검색 + 필터를 한 줄 중심으로 재배치해 세로 점유 면적을 크게 줄임.
- 621~900px 구간에서는 폭이 좁아지는 점을 고려해 컴팩트 2단 구조 유지.
- 다시 위로 올라오면 자동으로 원래 크기로 복귀.
- 검색/필터 변경은 스크롤 유지, 전체 초기화만 최상단 이동하는 기존 동작 유지.
- Worker 변경 없음.


## v24.4 빈 결과 버튼 / 컴팩트 sticky 안정화
- 빈 결과 화면의 `날짜 유형 해제` / `전체 보기` 등 액션 버튼 높이·패딩·모서리·정렬을 동일하게 통일.
- 기존 `.empty-reset-btn`의 별도 margin/padding 규칙 때문에 버튼 크기가 달라 보이던 문제를 최종 우선순위 CSS로 보정.
- PC 컴팩트 검색영역의 기준점을 toolbar 자체가 아닌 별도 zero-height anchor로 분리.
- 컴팩트 전환/해제 임계값 사이에 hysteresis 간격을 두어 경계에서 위아래로 조금 스크롤할 때 깜빡이는 현상 방지.
- resize 시 120ms 디바운스로 임계값 재계산.
- Worker 변경 없음.


## v24.5 디자인 정돈
- PC 컴팩트 검색영역에서 검색창 비중을 키우고 각 필터 폭/간격/높이를 한 단계 축소.
- 카드형/목록형/연도·월형의 정보 흐름을 `날짜 → 제목 → 출처` 기준으로 맞춤.
- 목록형에서도 출처를 한 줄 메타정보로 표시하고, 연도·월형에도 출처 한 줄을 추가.
- 일반동영상/Shorts 아이콘은 기능은 유지하되 크기와 배경 농도를 살짝 낮춰 콘텐츠보다 먼저 튀지 않도록 조정.
- 카드 hover의 떠오름/그림자 강도를 줄여 아카이브 느낌을 강화.
- 관리자 화면의 카드 여백·모서리·테두리 강도를 줄이고 중첩 박스의 시각적 무게를 완화.
- Worker 변경 없음.


## v24.6 연도 필터 카운트 정합성
- 연도·월 화면의 연도 칩 카운트를 전체 데이터 기준이 아니라 `현재 적용 중인 다른 필터` 기준으로 계산.
- 예: `동영상 타입: Shorts`가 켜진 상태에서 2011 칩은 2011년 Shorts 개수만 표시.
- 연도 칩의 `전체` 역시 현재 비연도 필터 조건에 맞는 전체 결과 수를 표시.
- 결과가 없는 연도는 연도·월 빠른 이동 칩에서 숨기되, 현재 선택된 연도는 0건이어도 남겨 원인을 확인할 수 있게 함.
- 연도 select의 괄호 안 카운트도 같은 기준으로 동기화.
- Worker 변경 없음.


## v24.7 UI 회귀 복구 + 컴팩트 모드 안정화
- PC 컴팩트 검색영역을 native sticky 높이 변경 방식에서 `고정(fixed) + 동일 높이 placeholder` 방식으로 변경.
- 컴팩트 전환 시 원래 검색영역 자리를 placeholder가 유지하므로 레이아웃 높이가 바뀌지 않아 스크롤 임계점에서 일반/컴팩트 모드가 반복 전환되는 현상을 방지.
- 진입/해제 기준에 54px 완충 구간을 유지하여 경계 스크롤에 추가 안정성 부여.
- 컴팩트 검색영역이 떠 있는 동안 연도·월 sticky 연도 제목은 그 아래에서 고정되도록 위치 조정.
- v23.3에서 적용했던 제목/날짜/출처의 가벼운 chip-like 타이포그래피 규칙 복구.
- v23.4에서 적용했던 혼합연도 카드의 날짜 3열 배치 복구: 연도 요약은 한 줄, 날짜는 한 줄에 최대 3개.
- Worker 변경 없음.


## v24.8 컴팩트 연도 필터 잘림 수정
- 컴팩트 모드에서 `전체 연도 (개수)` 텍스트가 잘리는 문제 수정.
- 연도 필터 최소 폭을 넓히고, 우측 필터 그룹 전체 폭을 소폭 확대.
- 검색창 영역은 조금 줄여 전체 한 줄 균형 유지.
- select 내부 텍스트가 화살표와 겹치지 않도록 우측 여백 보정.
- Worker 변경 없음.


## v24.9 컴팩트 필터 라벨 정리
- v24.8에서 연도 필터 폭을 억지로 넓히면서 검색창과 다른 필터가 답답해진 문제를 되돌림.
- 컴팩트 모드에서는 select 문구 자체를 짧게 표시:
  - `전체 연도 (611)` → `전체 연도`
  - `2011 (6)` → `2011년`
  - `전체 날짜 유형` → `전체 날짜`
  - `원본 날짜 최신순` → `원본 최신순`
- 컴팩트 모드를 벗어나면 연도 필터의 contextual count 표시는 다시 복구.
- 연도별 실제 개수는 연도·월 빠른 이동 칩에서도 계속 확인 가능.
- Worker 변경 없음.


## v24.10 컴팩트 연도 카운트 표기 복구
- 컴팩트 모드에서도 `전체 연도 (611)`, `2011 (6)`처럼 기존 연도 카운트 표기를 그대로 유지.
- v24.9의 축약 라벨 처리 제거.
- 연도 select 폭만 충분히 확보하고 다른 필터 폭을 미세 조정해 검색창이 과도하게 줄지 않도록 균형 재조정.
- Worker 변경 없음.


## v24.11 필터 정렬 / 컴팩트 날짜 라벨
- 일반 모드에서 `필터 초기화` 버튼을 필터 행의 오른쪽 끝으로 정렬.
- 컴팩트 모드에서는 `전체 날짜 유형`을 `전체 날짜`로 축약해 말줄임 없이 표시.
- 컴팩트 모드를 벗어나면 다시 `전체 날짜 유형`으로 복구.
- 연도 카운트 표기(`전체 연도 (611)`, `2011 (6)`)는 그대로 유지.
- Worker 변경 없음.


## v25.0 사용자 관점 1차 안정화
적용 범위: 1 → 2 → 5 → 6 → 3 → 17

- PC 컴팩트 검색바: sentinel + IntersectionObserver 방식으로 전환해 경계 깜빡임 방지.
- 다중 날짜: 모든 카드형에서 첫 3개 날짜를 한 줄 3열로 통일. 모바일도 3개 기준.
- 모바일 필터: 현재 5개 필터 기준으로 2열 재정렬, 정렬/초기화는 전체 폭.
- 필터 정합성: 실제 결과와 연도 select/연도월 카운트가 동일한 공통 필터 판정 함수 사용.
- Shorts 검토: 관리자 `타입 확인 필요` 보기, 확신도/판별 근거 표시, 낮은 확신만 모아 확인.
- Shorts 동기화: 다음 동기화부터 자동 판별 확신도/근거를 videos.json에 저장. 수동 지정은 유지.
- CSS 정리: v24.2~v24.11 후반 누적 override를 제거하고 v25.0 단일 최종 블록으로 통합.
- Worker 변경 있음.


## v25.1 컴팩트 날짜 글자 + Shorts 검토 UX 개선
- PC 컴팩트 모드의 `전체 날짜` select 글자 크기를 모바일과 비슷한 작은 크기로 조정.
- 관리자에서 추상적인 `확신 높음/보통/낮음` 표시 제거.
- 자동 판별 결과는 `자동 판별`, 사람이 확인해야 하는 경우만 `확인 필요`로 표시.
- 확인 필요 항목에는 왜 자동 확정하지 못했는지 구체적인 사유 표시.
- 자동 판별이 실제 영상과 맞으면 `현재 판별 확정` 버튼으로 관리자 확인 완료 처리 가능.
- 확인 완료된 타입은 `videoFormatSource=confirmed`로 저장되어 이후 YouTube 동기화에서도 유지.
- 일반동영상/Shorts 버튼으로 직접 변경하면 기존처럼 수동 지정으로 저장.
- 확인 작업도 관리자 변경 이력에 기록되고 되돌리기 지원.
- Worker 변경 있음.
